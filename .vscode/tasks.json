{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start postgres",
            "type": "shell",
            "command": "database:start",
            "runOptions": {
                "runOn": "folderOpen",
            }
        }
    ]
}